name: My First CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello from my first CI/CD pipeline!"

      - name: Show current directory
        run: pwd

      - name: List files
        run: ls
